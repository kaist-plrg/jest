#!/bin/bash
if [ -z "$IRES_HOME" ]; then
  echo "You need to set IRES_HOME to the directory containing IRES"
  exit 1
fi

ASSEMBLY_JAR="${IRES_HOME}/target/scala-2.13/IRES-assembly-1.0.jar"
if [ ! -f "${ASSEMBLY_JAR}" ]; then
  cd "${IRES_HOME}" && sbt assembly
fi
java -jar ${ASSEMBLY_JAR} $@
