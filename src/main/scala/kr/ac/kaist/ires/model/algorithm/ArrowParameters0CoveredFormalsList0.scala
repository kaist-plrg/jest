package kr.ac.kaist.ires.model

import kr.ac.kaist.ires.ir._
import kr.ac.kaist.ires.ir.Parser._

object ArrowParameters0CoveredFormalsList0 {
  val length: Int = 0
  val func: Func = Func("""ArrowParameters0CoveredFormalsList0""", List(Id("""this"""), Id("""BindingIdentifier""")), None, IReturn(ERef(RefId(Id("""this""")))))
  /* Beautified form:
  "ArrowParameters0CoveredFormalsList0" (this, BindingIdentifier) => return this
  */
}
