package kr.ac.kaist.ires.model

import kr.ac.kaist.ires.ir._
import kr.ac.kaist.ires.ir.Parser._

object LabelledStatement0TopLevelLexicallyDeclaredNames0 {
  val length: Int = 0
  val func: Func = Func("""LabelledStatement0TopLevelLexicallyDeclaredNames0""", List(Id("""this"""), Id("""LabelIdentifier"""), Id("""LabelledItem""")), None, IReturn(EList(List())))
  /* Beautified form:
  "LabelledStatement0TopLevelLexicallyDeclaredNames0" (this, LabelIdentifier, LabelledItem) => return (new [])
  */
}
