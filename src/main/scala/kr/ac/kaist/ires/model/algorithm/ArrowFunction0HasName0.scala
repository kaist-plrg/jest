package kr.ac.kaist.ires.model

import kr.ac.kaist.ires.ir._
import kr.ac.kaist.ires.ir.Parser._

object ArrowFunction0HasName0 {
  val length: Int = 0
  val func: Func = Func("""ArrowFunction0HasName0""", List(Id("""this"""), Id("""ArrowParameters"""), Id("""ConciseBody""")), None, IReturn(EBool(false)))
  /* Beautified form:
  "ArrowFunction0HasName0" (this, ArrowParameters, ConciseBody) => return false
  */
}
