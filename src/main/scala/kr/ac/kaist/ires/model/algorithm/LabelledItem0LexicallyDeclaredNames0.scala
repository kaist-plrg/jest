package kr.ac.kaist.ires.model

import kr.ac.kaist.ires.ir._
import kr.ac.kaist.ires.ir.Parser._

object LabelledItem0LexicallyDeclaredNames0 {
  val length: Int = 0
  val func: Func = Func("""LabelledItem0LexicallyDeclaredNames0""", List(Id("""this"""), Id("""Statement""")), None, IReturn(EList(List())))
  /* Beautified form:
  "LabelledItem0LexicallyDeclaredNames0" (this, Statement) => return (new [])
  */
}
