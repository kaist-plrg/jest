package kr.ac.kaist.ires.model

import kr.ac.kaist.ires.ir._
import kr.ac.kaist.ires.ir.Parser._

object ConciseBody0LexicallyScopedDeclarations0 {
  val length: Int = 0
  val func: Func = Func("""ConciseBody0LexicallyScopedDeclarations0""", List(Id("""this"""), Id("""AssignmentExpression""")), None, IReturn(EList(List())))
  /* Beautified form:
  "ConciseBody0LexicallyScopedDeclarations0" (this, AssignmentExpression) => return (new [])
  */
}
